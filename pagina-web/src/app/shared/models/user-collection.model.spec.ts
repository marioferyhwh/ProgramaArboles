import { UserCollectionModel } from "./user-collection.model";

describe("UserCollectionModel", () => {
  it("should create an instance", () => {
    expect(new UserCollectionModel()).toBeTruthy();
  });
});
